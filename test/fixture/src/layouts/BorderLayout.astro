---
import Layout from './Layout.astro';
export interface Props {
	title: string;
}

const { title } = Astro.props;
---

<Layout {title}>
	<slot />
	<script>
		console.log('[test] loaded');
		document.addEventListener('astro:after-swap', () => {
			console.log('[test] after-swap');
		});
	</script>
</Layout>
